import { Typography } from '@mui/material';
import * as React from 'react';

export default function SubsiteProjects() {
	return (
		<React.Fragment>
			<Typography>Hi, I'm the project screen!</Typography>	
		</React.Fragment>
	)
}